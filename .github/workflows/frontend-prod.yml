name: "Frontend: Prod"

on:
  push:
    branches: [main]
    paths:
      - "deployments/frontend/prod/**"
      - "modules/s3-frontend/**"
      - "modules/cloudfront/**"
      - "modules/secrets-manager/**"
  pull_request:
    branches: [main]
    paths:
      - "deployments/frontend/prod/**"
      - "modules/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/_terraform-reusable.yml
    with:
      working_directory: deployments/frontend/prod
      environment: prod
    secrets:
      AWS_ROLE_ARN: ${{ secrets.PROD_TF_AWS_ROLE_ARN }}
