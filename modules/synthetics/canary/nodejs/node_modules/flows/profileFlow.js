"use strict";
// TODO: replace this stub with your actual profileFlow implementation
const profileFlow = {
  run: async (deps) => { deps.log.info("profileFlow.run — stub"); },
  runGetAndUpdate: async (deps) => { deps.log.info("profileFlow.runGetAndUpdate — stub"); },
  runCreate: async (deps) => { deps.log.info("profileFlow.runCreate — stub"); },
  runListIfNeeded: async (deps) => { deps.log.info("profileFlow.runListIfNeeded — stub"); },
  runCreateGetUpdate: async (deps, opts) => { deps.log.info("profileFlow.runCreateGetUpdate — stub"); },
  runCreateConversationDefault: async (deps) => { deps.log.info("profileFlow.runCreateConversationDefault — stub"); },
  runCreateAppealLetter: async (deps) => { deps.log.info("profileFlow.runCreateAppealLetter — stub"); },
  runCreateAndGet: async (deps) => { deps.log.info("profileFlow.runCreateAndGet — stub"); },
  runUpdate: async (deps) => { deps.log.info("profileFlow.runUpdate — stub"); },
};
module.exports = profileFlow;
