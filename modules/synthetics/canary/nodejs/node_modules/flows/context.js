"use strict";

/**
 * createRunContext — shared mutable state passed between flows.
 * Replace with your actual context structure.
 */
function createRunContext() {
  return {
    authToken:                  null,
    patientUid:                 null,
    caseUid:                    null,
    appointmentUid:             null,
    conversationTemplateUid:    null,
    appealLetterTemplateUid:    null,
    quickNoteUid:               null,
    conversationUid:            null,
  };
}

module.exports = { createRunContext };
